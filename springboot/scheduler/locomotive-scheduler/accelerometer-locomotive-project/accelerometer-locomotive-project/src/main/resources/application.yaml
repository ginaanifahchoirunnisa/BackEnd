server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_locomotive
    username: postgres
    password: ginza
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate-ddl-auto: update
    show-sql: true
